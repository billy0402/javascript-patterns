/*
 * 豐富而一致的空格
 * 讓程式碼容易閱讀
 * 讀起來更有喘息空間
 */
var d = 0,
    a = b + 1;
if (a && b && c) {
    d = a % c;
    a += d;
}

/*
 * 反模式:
 * 缺少空格或不一致的空格
 * 程式碼變得令人困惑
 */
var d= 0,
    a =b+1;
if (a&& b&&c) {
d=a %c;
    a+= d;
}
